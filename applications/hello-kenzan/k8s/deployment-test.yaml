apiVersion: v1
kind: Service
metadata:
  name: hello-kenzan-test
  labels:
    app: hello-kenzan-test
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: hello-kenzan-test
    tier: hello-kenzan-test
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hello-kenzan-test
  labels:
    app: hello-kenzan-test
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: hello-kenzan-test
        tier: hello-kenzan-test
    spec:
      containers:
      - image: 127.0.0.1:30400/hello-kenzan:$BUILD_TAG
        name: hello-kenzan-test
        ports:
        - containerPort: 80
          name: hello-kenzan-test